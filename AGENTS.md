# AGENTS

Start here before exploring the repository in depth.

## First File To Read
- `PROJECT_CONTEXT.md`

## Purpose
This repository already has a compressed context file intended to save tokens and reduce repeated repo exploration.
Read `PROJECT_CONTEXT.md` first, then inspect code only where needed.

## Working Rules
- Treat `PROJECT_CONTEXT.md` as the fast-start summary.
- If code and `PROJECT_CONTEXT.md` differ, trust the code and update `PROJECT_CONTEXT.md` in the same task.
- Do not add parallel architectures or duplicate flows when extending the app.
- Keep frontend and backend contracts aligned.
- Keep guest mode working unless explicitly asked to remove it.
- Update `README.md` when setup, env vars, or commands change.

## Mandatory Maintenance
When making meaningful changes, update:
- `PROJECT_CONTEXT.md`
- `README.md` if developer-facing setup or usage changed
